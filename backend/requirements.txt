# Python dependencies for backend/ (root requirements.txt includes this file via -r).
#
# Generated to match imports under backend/; run from repo root:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# Core framework
fastapi==0.118.2
uvicorn[standard]==0.37.0
python-multipart==0.0.20

# Data processing & analysis
pandas==2.3.3
numpy==1.26.4
openpyxl==3.1.5
scipy==1.16.2

# Pydantic & settings
pydantic==2.12.0
pydantic-settings==2.11.0

# AI / LLM (langgraph pulls langchain-core; pinned for reproducibility)
tiktoken==0.9.0
anthropic==0.40.0
openai==1.59.8
langgraph==0.2.45
langchain-core==0.3.63
langgraph-checkpoint==2.1.2
langgraph-sdk==0.1.74

# PostgreSQL (AACT live DB + app chat persistence)
asyncpg==0.30.0
psycopg2-binary
sqlalchemy[asyncio]==2.0.36
alembic==1.16.1
slowapi==0.1.9
greenlet==3.2.2

# AWS (S3 regulatory data bundle in prod)
boto3==1.35.99

# HTTP clients & retries
httpx==0.28.1
requests==2.32.5
certifi>=2024.8.30
tenacity==8.5.0

# Async
asyncio-throttle==1.0.2

# Config & env
python-dotenv==1.1.1

# System / geo / fuzzy matching
psutil==7.1.0
pgeocode==0.5.0
fuzzywuzzy==0.18.0
thefuzz==0.22.1
python-Levenshtein==0.27.3

# Cache
redis==5.2.0

# Documents & HTML parsing
PyMuPDF==1.26.5
python-docx==1.1.2
beautifulsoup4==4.12.2

# Logging
loguru==0.7.2

# WebSockets (API + pytest e2e)
websockets==15.0.1

# Date/time (used by pandas and elsewhere)
python-dateutil==2.9.0.post0
pytz==2025.2

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# Decision-cut comparison
deepdiff==6.7.1
